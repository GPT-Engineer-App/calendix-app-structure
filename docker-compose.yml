version: '3.8'

services:
  app:
    build: .
    ports:
      - '3000:3000'
    environment:
      - VITE_SUPABASE_PROJECT_URL=${VITE_SUPABASE_PROJECT_URL}
      - VITE_SUPABASE_API_KEY=${VITE_SUPABASE_API_KEY}
    volumes:
      - .:/app
      - /app/node_modules